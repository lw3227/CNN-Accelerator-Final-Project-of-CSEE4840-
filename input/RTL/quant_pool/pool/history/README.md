# Legacy Pool RTL

这里存放已经退出当前 active 主链的 legacy pool RTL。

当前 active pool 路径请优先使用：

- `input/RTL/quant_pool/pool/pool_stream_top.v`
- `input/RTL/quant_pool/pool/pool_core.v`

这里的文件仅保留给历史对照或 compatibility 参考。
